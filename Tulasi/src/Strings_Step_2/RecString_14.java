package Strings_Step_2;
//16.WAP to reverse String using recursive manner?
public class RecString_14 {
    public static void main(String[] args) {
        String obj = new String();
        String stringName = "Recursion manner";
        System.out.println(stringName);
        //String string1 = obj.recursion(stringName);
        System.out.println();
      /*  public String ReverseString(String stringName)
        {
            if (stringName == null || stringName.length <= 1)  // if length is less or equal 1 then also returns
                return stringName;

            return ReverseString(stringName.substring(1)) +stringName[0];
        }*/
}}
